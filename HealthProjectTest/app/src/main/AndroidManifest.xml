<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.videoview">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.SEND_SMS"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/health_care_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/health_care_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".map_activities.UserMapsActivity"
            android:label="@string/title_activity_user_maps">

        </activity>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".map_activities.PharmacyMapsActivity"
            android:label="@string/title_activity_pharmacy_maps" />
        <activity android:name=".activities.DoctorActivity" />
        <activity android:name=".activities.DoctorProfileActivity" />
        <activity android:name=".activities.BloodActivity" />
        <activity android:name=".activities.BloodProfileActivity" />
        <activity android:name=".activities.PharmacyActivity" />
        <activity android:name=".activities.AmbulanceActivity" />
        <activity android:name=".activities.PharmacyProfileActivity" />
        <activity android:name=".activities.AmbulanceProfileActivity" />
        <activity android:name=".activities.SignUpMedicalAssistantActivity" />
        <activity
            android:name=".activities.SignUpBloodBankActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".activities.SignUpAmbulanceActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".activities.SplashActivity" />
        <activity android:name=".activities.MainActivity" />
        <activity
            android:name=".activities.SignupDoctorActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".activities.SignupGuser_Activity"
            android:windowSoftInputMode="adjustPan" >



        </activity>
        <activity
            android:name=".activities.SignupPharmacyActivity"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>


        </activity>
        <activity android:name=".activities.UserSelectionActivity"></activity>
        <activity android:name=".activities.OtpActivity" />
        <activity android:name=".activities.TestActivity" />
        <activity android:name=".activities.SliderActivity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>